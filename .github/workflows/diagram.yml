name: Create diagram
on:
  workflow_dispatch: {}
  push:
    branches:
      - master
jobs:
  get_data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@master
      - name: Update diagram
        uses: Refugee-Stories-BW-PTCT/back-endr@master
        with:
          excluded_paths: "ignore,node_modules"
